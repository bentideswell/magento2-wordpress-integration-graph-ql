
query {
    term (
        id: 1
        withTaxonomy: "category"
    ) {
        id
        name
        slug
        taxonomy_id
        taxonomy
        description
        parent
        count
    }
}
